apiVersion: apps/v1
kind: Deployment
metadata:
  name: django
  namespace: myapp
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: django
          imagePullPolicy: IfNotPresent
